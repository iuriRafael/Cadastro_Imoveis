<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<style>
  /* Estilos gerais para o formulário */
/* Estilos gerais para o formulário */

.tudo{
  align-items: center;
  justify-content: center;
  display: flex;
  margin-top: 7vw;
}
form {
  display: flex;
  flex-direction: column;
  max-width: 400px;
  margin: 0 auto;
  text-align: center; /* Centraliza o conteúdo */
  font-family: Arial, sans-serif; /* Melhora a fonte */
}

.input-box {
  margin-bottom: 20px; /* Aumenta o espaço entre os campos */
}

.label {
  font-weight: bold;
  font-size: 16px; /* Aumenta o tamanho da fonte para rótulos */
}

input[type="text"],
input[type="tel"] {
  width: 100%;
  padding: 10px;
  margin-top: 5px;
  border: 1px solid #ccc;
  border-radius: 5px;
  font-size: 16px; /* Aumenta o tamanho da fonte para campos de entrada */
}

button[type="submit"] {
  background-color: #007bff;
  color: #fff;
  padding: 10px 20px;
  border: none;
  border-radius: 5px;
  cursor: pointer;
  font-size: 18px; /* Aumenta o tamanho da fonte para o botão */
}

/* Responsividade para telas menores */
@media (max-width: 768px) {
  form {
    max-width: 100%;
  }
}

@media (max-width: 480px) {
  input[type="text"],
  input[type="tel"] {
    width: 100%;
  }
}


</style>
<body style="height: 100vh;">
  <div class="tudo">
    <form action="/atualizar" method="post">
      <input type="hidden" name="id" value="<%= publicacao.id %>">
      <div class="input-box">
        <label for="descricao">Descrição:</label>
        <input type="text" id="descricao" name="description" value="<%= publicacao.description %>">
      </div>
    
      <div class="input-box">
        <label for="endereco">Endereço:</label>
        <input type="text" id="endereco" name="address" value="<%= publicacao.address %>">
      </div>
    
      <div class="input-box">
        <label for="telefone">Telefone:</label>
        <input type="tel" id="telefone" name="phone_number" value="<%= publicacao.phone_number %>">
      </div>
    
      <div class="input-box">
        <label for="valor">Valor:</label>
        <input type="text" id="valor" name="value" value="<%= publicacao.value %>">
      </div>
    
      <!-- Adicione outros campos de edição conforme necessário -->
    
      <button type="submit">Salvar</button>
    </form>
  </div>
  
    
</body>
</html>